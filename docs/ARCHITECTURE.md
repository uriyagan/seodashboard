# SEO Dashboard — ארכיטקטורה

> מסמך זה מתעד את הארכיטקטורה של המערכת: רכיבים, זרימות נתונים והחלטות תכנון.
> מתעדכן באופן שוטף.

**עודכן לאחרונה:** 2026-07-21

---

## תרשים רכיבים (ברמה גבוהה)

```
                         ┌──────────────────────────────────────┐
                         │        seo.uriyaganor.com             │
                         │  Cloudflare Worker (Hono)             │
                         │   ├─ SPA (React+Vite) — assets        │
                         │   └─ /api/* — שרת מאובטח               │
                         │   סוויצ'ר פרויקטים · עורך פוסטים        │
                         │   גישת אדמינים בלבד (2)                │
                         └───────────────┬──────────────────────┘
                                         │  (Worker /api = שרת מאובטח, סודות=Secrets)
        ┌──────────────┬─────────────────┼──────────────────┬─────────────────┐
        │              │                 │                  │                 │
 ┌──────▼──────┐ ┌─────▼──────┐   ┌───────▼───────┐  ┌───────▼──────┐  ┌───────▼───────┐
 │  Supabase   │ │  Gemini    │   │ Nano Banana 2 │  │   Resend     │  │  WordPress    │
 │ DB+Auth+    │ │  (טקסט)    │   │ (תמונות AI)   │  │   (מייל)     │  │  אתרי לקוחות  │
 │ Storage     │ │            │   │  Gemini Image │  │              │  │  REST API     │
 └─────────────┘ └────────────┘   └───────────────┘  └──────────────┘  └───────┬───────┘
                                                                                │
                                                             ┌──────────────────▼──────────────────┐
                                                             │ REST /wp-json/wp/v2 (Application PW)  │
                                                             │ posts · categories · tags · media    │
                                                             │ + mu-plugin ל-Yoast (focuskw/title/  │
                                                             │   metadesc — קריאה/כתיבה)            │
                                                             └──────────────────────────────────────┘
```

---

## רכיבים

### 1. Frontend + Backend — Next.js (Vercel)
- **דומיין:** `seo.uriyaganor.com`.
- **גישה:** אדמינים בלבד, 2 משתמשים (Supabase Auth + allowlist).
- **Backend:** Next.js **API Routes** משמשות כשכבת שרת מאובטחת — כל הקריאות לשירותים החיצוניים (Gemini, Resend, WordPress, service-role של Supabase) עוברות דרך השרת בלבד. מפתחות ה-API לעולם לא נחשפים ללקוח.
- **UI:** Tailwind + shadcn/ui. **עורך:** TipTap (HTML נקי תואם WordPress).

### 2. Supabase — נתונים, אימות ואחסון
- **DB (Postgres):** projects, posts, ideas, images, prompts, admins, settings.
- **Auth:** הגבלה ל-2 אדמינים.
- **Storage:** גיבוי תמונות שנוצרו.
- **RLS:** גישה לאדמינים המורשים בלבד.
- **הצפנה:** פרטי חיבור WordPress (Application Password) נשמרים מוצפנים (pgsodium / הצפנה בצד שרת).

### 3. Gemini — טקסט
- כתיבת פוסטים מפרומפט קבוע + נושא; יצירת 10 רעיונות מרשימת כותרות קיימות.

### 4. Nano Banana 2 — תמונות
- מודל התמונות של Gemini. יצירת תמונה ראשית + תמונות גוף לפי פרומפט כללי קבוע + הנחיה ספציפית.

### 5. Resend — התראות מייל
- מייל ל-2 האדמינים על אירועים נבחרים.

### 6. WordPress — אתרי לקוחות (מרובה)
- חיבור לכל אתר דרך **REST API** עם **Application Passwords**.
- ניהול פוסטים, קטגוריות, תגיות, מדיה.
- **mu-plugin** ייעודי בכל אתר לחשיפת שדות Yoast ל-REST (כתיבה).

---

## זרימות מפתח

### א. יצירת פוסט עם AI
```
משתמש בוחר פרויקט → מזין נושא/רעיון
  → API Route → Gemini (פרומפט קבוע) → טקסט + מבנה
  → Nano Banana 2 (פרומפט תמונה) → תמונות
  → העלאת תמונות ל-WP Media (+ גיבוי Supabase)
  → נחיתה בעורך לאישור → דחיפה ל-WP (טיוטה/פרסום) + שדות Yoast
```

### ב. "הצע לי רעיונות חדשים"
```
API Route → משיכת כל כותרות הפוסטים מ-WP REST
  → Gemini: "הנה הכותרות הקיימות, הצע 10 חדשים שלא נכתבו"
  → הצגת 10 רעיונות → לחיצה על רעיון → זרימה א'
```

### ג. סנכרון Yoast
```
עריכת שדות בעורך → API Route → WP REST (mu-plugin)
  → כתיבת _yoast_wpseo_focuskw / _title / _metadesc
```

---

## החלטות ארכיטקטורה (ADR)

| # | החלטה | נימוק | תאריך |
|---|--------|--------|-------|
| 1 | אחסון על Supabase | קונקטור זמין, Postgres + Auth + Storage | 2026-07-21 |
| 2 | Gemini כמנוע AI טקסט | דרישת המשתמש | 2026-07-21 |
| 3 | Nano Banana 2 לתמונות | דרישת המשתמש (דרך Gemini API) | 2026-07-21 |
| 4 | Resend להתראות | קונקטור זמין, דרישת המשתמש | 2026-07-21 |
| 5 | React+Vite (SPA) + Cloudflare Workers/Hono | הדומיין על Cloudflare + דרישת Workers; דאשבורד אדמין לא צריך SSR | 2026-07-21 (מעודכן) |
| 5b | Hosting מלא על Cloudflare (לא Vercel) | הדומיין מנוהל ב-Cloudflare, שימוש ב-Workers, פלטפורמה אחת | 2026-07-21 |
| 5c | גיבוי ל-GitHub (uriyagan/seodashboard) | דרישת המשתמש — גיבוי שוטף | 2026-07-21 |
| 6 | חיבור WP: REST + Application Passwords | סטנדרטי, מובנה, ללא תלות ב-plugin חיצוני מלבד snippet ל-Yoast | 2026-07-21 (מוצע) |
| 7 | Yoast דרך mu-plugin | WP REST לא מאפשר כתיבה ל-meta מוגן כברירת מחדל | 2026-07-21 (מוצע) |
| 8 | עורך TipTap (HTML) | פתרון ריאלי ל"עורך כמו WP" בלי לשכפל Gutenberg | 2026-07-21 (מוצע) |

---

## סוגיות פתוחות
- רמת נאמנות העורך (TipTap/HTML מול בלוקים של Gutenberg).
- Workflow פרסום (טיוטה / פרסום ישיר / per-post).
- אירועים המזמנים התראה.
- שיטת הצפנת סודות ה-WP (pgsodium מול KMS/סביבת שרת).
